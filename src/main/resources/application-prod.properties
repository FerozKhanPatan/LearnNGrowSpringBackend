# Production Configuration for Render/Supabase

# Database - Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?user=postgres.mjffrfzuxrxeukszsols&password=1234@#Abcd@#
spring.datasource.username=postgres.mjffrfzuxrxeukszsols
spring.datasource.password=1234@#Abcd@#
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server
server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.context-path=/

# JWT
jwt.secret=elearningplatformsecretkey2024verysecure
jwt.expiration=86400000

# CORS
cors.allowed-origins=http://localhost:3000,https://www.learnnbyte.com
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# Logging
logging.level.root=WARN
logging.level.com.elearning=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
